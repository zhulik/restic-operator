apiVersion: restic.zhulik.wtf/v1
kind: Repository
metadata:
  labels:
    app.kubernetes.io/name: restic-operator
    app.kubernetes.io/managed-by: kustomize
  name: repository-sample
spec:
  # TODO(user): Add fields here
